DBSCHEMA_CATALOGS=Identifies the physical attributes associated with catalogs accessible from the provider.
DISCOVER_DATASOURCES=Returns a list of XML for Analysis data sources available on the server or Web Service.
DISCOVER_ENUMERATORS=Returns a list of names, data types, and enumeration values for enumerators supported by the provider of a specific data source.
DISCOVER_KEYWORDS=Returns an XML list of keywords reserved by the provider.
DISCOVER_LITERALS=Returns information about literals supported by the provider.
DISCOVER_PROPERTIES=Returns a list of information and values about the requested properties that are supported by the specified data source provider.
DISCOVER_SCHEMA_ROWSETS=Returns the names, values, and other information of all supported RequestType enumeration values.
DISCOVER_XML_METADATA=Returns an XML document describing a requested object. The rowset that is returned always consists of one row and one column.

AccessEnum=The read/write behavior of a property.

AuthenticationModeEnum=Specification of what type of security mode the data source uses.
AuthenticationModeEnum.Unauthenticated=No user ID or password needs to be sent.
AuthenticationModeEnum.Authenticated=User ID and Password must be included in the information required for the connection.
AuthenticationModeEnum.Integrated=The data source uses the underlying security to determine authorization, such as Integrated Security provided by Microsoft Internet Information Services (IIS).

ProviderTypeEnum=The types of data supported by the provider.
ProviderTypeEnum.TDP=Tabular data provider.
ProviderTypeEnum.MDP=Multidimensional data provider.
ProviderTypeEnum.DMP=Data mining provider. A DMP provider implements the OLE DB for Data Mining specification.

TreeOpEnum=Bitmap which controls which relatives of a member are returned
TreeOpEnum.MDTREEOP_CHILDREN=Tree operation which returns only the immediate children.
TreeOpEnum.MDTREEOP_SIBLINGS=Tree operation which returns members on the same level.
TreeOpEnum.MDTREEOP_PARENT=Tree operation which returns only the immediate parent.
TreeOpEnum.MDTREEOP_SELF=Tree operation which returns itself in the list of returned rows.
TreeOpEnum.MDTREEOP_DESCENDANTS=Tree operation which returns all of the descendants.
TreeOpEnum.MDTREEOP_ANCESTORS=Tree operation which returns all of the ancestors.
